<UserControl x:Class="Quickbeam.IronPythonConsole.ReplGrid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:console="clr-namespace:Quickbeam.PythonConsoleControl;assembly=Quickbeam.PythonConsoleControl"
        mc:Ignorable="d" 
        d:DesignHeight="500" d:DesignWidth="800">
    <!-- 
	Copyright (c) 2010 Joe Moorhouse
    -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <console:IronPythonConsoleControl Name="Console" />
        </Grid>
        <GridSplitter Grid.Row="1" Height="10" VerticalAlignment="Center" HorizontalAlignment="Stretch"></GridSplitter>
        <DockPanel Grid.Row="2">
            <ToolBar DockPanel.Dock="Top" ToolBarTray.IsLocked="True" Background="#444444">
                <ToolBar.Resources>
                    <Style TargetType="{x:Type Image}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}, AncestorLevel=1}, Path=IsEnabled}" Value="False">
                                <Setter Property="Opacity" Value="0.30" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ToolBar.Resources>
                <Button Click="OpenFileClick" ToolTip="Open">
                    <Image Source="Images/Open.png" Height="16"/>
                </Button>
                <Button Click="SaveFileClick" ToolTip="Save">
                    <Image Source="Images/Save.png" Height="16"/>
                </Button>
                <Separator/>
                <Button Command="Undo" ToolTip="Undo">
                    <Image Source="Images/Undo.png" Height="16"/>
                </Button>
                <Button Command="Redo" ToolTip="Redo">
                    <Image Source="Images/Redo.png" Height="16"/>
                </Button>
                <Separator/>
                <CheckBox IsChecked="{Binding ElementName=textEditor,Path=WordWrap}" ToolTip="Word Wrap">
                    <Image Source="Images/WordWrap.png" Height="16"/>
                </CheckBox>
                <CheckBox IsChecked="{Binding ElementName=textEditor,Path=ShowLineNumbers}" ToolTip="Show Line Numbers">
                    <TextBlock Width="16" TextAlignment="Center">#</TextBlock>
                </CheckBox>
                <CheckBox IsChecked="{Binding ElementName=textEditor,Path=Options.ShowEndOfLine}" ToolTip="Show Newlines">
                    <TextBlock Width="16" TextAlignment="Center">¶</TextBlock>
                </CheckBox>
                <Separator/>
                <Button Click="RunClick" ToolTip="Run">
                    <Image Source="Images/Run.png" Height="16"/>
                </Button>
            </ToolBar>
            <avalonEdit:TextEditor DockPanel.Dock="Bottom"
			    Name="textEditor" Foreground="#c5c8c6" Background="#212121"
			    FontFamily="Consolas" FontSize="9pt"
                Text="# QuickPad:&#x0a;# - Write code or paste snippets, then press F5 to run.&#x0a;# - If code is selected, only the selection is run.&#x0a;" />
        </DockPanel>
    </Grid>
</UserControl>
