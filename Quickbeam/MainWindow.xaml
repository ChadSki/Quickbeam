<Window x:Class="Quickbeam.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quickbeam" Width="1000" Height="639"
        Background="SlateGray">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="800" MaxWidth="800" MinWidth="800" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="600" MaxHeight="600" MinHeight="600" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Column="0" Grid.Row="0" Click="btnHost_Click" Height="600" MaxHeight="600" MinHeight="600">Load Halo Here</Button>
    </Grid>
</Window>
